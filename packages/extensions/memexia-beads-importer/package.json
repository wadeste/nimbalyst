{
  "name": "@nimbalyst/extension-memexia-beads-importer",
  "version": "0.1.0",
  "description": "Built-in memexia beads (bb) importer for the Nimbalyst tracker",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.backend.config.ts",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^7.1.12"
  }
}
