# Hard-block installs with the wrong toolchain (see "engines" in package.json).
# npm 10 and npm 11 write structurally incompatible lockfiles for overrides;
# everything (local dev, CI, publish) must run Node 24 / npm 11.
engine-strict=true
